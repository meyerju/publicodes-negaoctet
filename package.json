{
  "name": "root",
  "version": "0.1.2",
  "type": "module",
  "workspaces": [
    "doc",
    "api",
    "rules"
  ],
  "scripts": {
    "build": "yarn --cwd rules build",
    "doc": "yarn build && yarn --cwd doc start",
    "predeploy": "yarn run build",
    "deploy": "cd doc && gh-pages -d build"
  },
  "repository": "git@github.com:incubateur-ademe/modele-numerique.git",
  "devDependencies": {
    "@incubateur-ademe/publicodes-tools": "^0.2.1"
  },
  "packageManager": "yarn@3.6.3"
}
